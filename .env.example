# Base URL of the Folio backend API (no trailing slash).
# iOS simulator can reach the docker stack on localhost; Android emulator uses 10.0.2.2.
# Production: https://folio.flowitup.com
EXPO_PUBLIC_API_BASE_URL=http://localhost:5000
